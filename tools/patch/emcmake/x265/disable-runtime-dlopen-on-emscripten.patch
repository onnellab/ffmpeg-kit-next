--- src/x265/source/encoder/api.cpp
+++ src/x265/source/encoder/api.cpp
@@ -1151,8 +1151,14 @@ const x265_api* x265_api_get(int bitDepth)
         else if (bitDepth == 8)
             libname = "libx265_main" ext;
         else
             return NULL;
 
+#if defined(__EMSCRIPTEN__)
+        /* FFmpegKit web dynamic builds link one static x265 archive. Do not try
+         * to dlopen native multilib names like libx265_main12.so in the browser. */
+        return NULL;
+#endif
+
         const x265_api* api = NULL;
         int reqDepth = 0;
 
@@ -1238,10 +1244,17 @@ const x265_api* x265_api_query(int bitDepth, int apiVersion, int* err)
         else if (bitDepth == 8)
             libname = "libx265_main" ext;
         else
         {
             if (err) *err = X265_API_QUERY_ERR_LIB_NOT_FOUND;
             return NULL;
         }
 
+#if defined(__EMSCRIPTEN__)
+        /* FFmpegKit web dynamic builds link one static x265 archive. Do not try
+         * to dlopen native multilib names like libx265_main12.so in the browser. */
+        if (err) *err = X265_API_QUERY_ERR_LIB_NOT_FOUND;
+        return NULL;
+#endif
+
         const x265_api* api = NULL;
         int reqDepth = 0;
         int e = X265_API_QUERY_ERR_LIB_NOT_FOUND;
